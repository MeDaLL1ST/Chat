<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ico1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ico1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_cr_ls" />
        <activity android:name=".activity_ch_email" />
        <activity android:name=".activity_ac" />
        <activity android:name=".activity_profile" />
        <activity android:name=".activity_private_chat" />
        <activity android:name=".activity_re" />
        <activity
            android:name=".activity_chat"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activity_signup" />
        <activity android:name=".signing" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>